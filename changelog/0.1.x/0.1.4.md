---
summary: "@cyanheads/mcp-ts-core ^0.9.7 → ^0.9.13; dep refresh; error code corrections; landing requireAuth"
breaking: false
security: false
---

# 0.1.4 — 2026-05-28

## Changed

- **`@cyanheads/mcp-ts-core`** ^0.9.7 → ^0.9.13 — picks up HTTP session-init gate (HTTP 400 on missing `Mcp-Session-Id`), configurable request-body cap (`MCP_HTTP_MAX_BODY_BYTES`, default 1 MiB, 413 on oversize), quieter 401/403/400/404 logging (warning level, no stack traces), and `GET /mcp` now surfaces `package.json` `keywords`.
- **`landing.requireAuth: false`** added to `createApp()` — explicitly opts the hosted server into public catalog inventory (tool list visible without auth), consistent with `MCP_AUTH_MODE=none` deployments.
- **`invalidParams` → `validationError`** in `ferry-service.ts` and two ferry tool error contracts — aligns with the framework's `ValidationError` code for malformed input.
- **`@biomejs/biome`** ^2.4.7 → ^2.4.16
- **`@types/node`** ^25.6.0 → ^25.9.1
- **`tsc-alias`** ^1.8.16 → ^1.8.17
- **`typescript`** ^5.9.3 → ^6.0.3
- **`vitest`** ^4.1.0 → ^4.1.7
- **Skills synced** — `git-wrapup`, `code-simplifier`, and skill refreshes from the 0.9.8–0.9.13 framework series.
