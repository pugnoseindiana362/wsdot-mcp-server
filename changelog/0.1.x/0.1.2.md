---
summary: "Fix all 6 traffic tools returning empty arrays, fix ferry routes field mapping, package.json standardization"
breaking: false
---

# 0.1.2 — 2026-05-23

## Fixed

- All 6 traffic tools (mountain passes, alerts, travel times, toll rates, border crossings, cameras) now return data — removed incorrect response wrapper unwrapping; WSDOT Traffic API returns flat arrays, not wrapped objects
- `get_ferry_routes` now returns route description and abbreviation — mapped to actual API fields (Description, RouteAbbrev) instead of non-existent RouteName/terminal ID fields
- Removed 8 dead `Raw*Response` wrapper types from traffic types

## Changed

- Scripts switched from tsx to bun run (removed tsx devDependency)
- Added funding block (GitHub Sponsors, Buy Me a Coffee)
- Added publish-mcp script
- manifest.json: added repository, homepage, license; fixed user_config env reference
- Dockerfile label ordering + description suffix aligned
- Fixed repository URL format and added packageManager field
